using UnityEngine;
using System.Collections;
using UnityEngine.Networking;

public class Player_Setup : NetworkBehaviour {

	// Use this for initialization
	void Start () {
		Debug.LogWarning("asfafsa");
		if(isLocalPlayer){
			
		}
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
